{
  "name": "we-backend-main",
  "version": "1.0.0",
  "description": "Full-stack application with separated frontend and backend",
  "scripts": {
    "dev": "npx concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:frontend": "npm --prefix frontend run dev",
    "dev:backend": "npm --prefix backend run dev",
    "build": "npm run install:all && npm run build:frontend && npm run generate:prisma",
    "build:frontend": "npm --prefix frontend run build",
    "generate:prisma": "npm --prefix backend run db:generate",
    "install:all": "npm install && npm --prefix frontend install && npm --prefix backend install",
    "start": "node backend/src/index.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "workspaces": [
    "frontend",
    "backend"
  ]
}
